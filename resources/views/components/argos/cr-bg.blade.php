{{-- Decorative control-room decklayers: glow + dot grid.
     Use inside any .cr-scope container. Both use border-radius:inherit so they clip
     to the parent's rounded corners without requiring overflow:hidden on the parent. --}}
<div class="cr-glow" aria-hidden="true"></div>
<div class="cr-grid" aria-hidden="true"></div>
